@model IEnumerable<SacramentPlanner.Models.MeetingSpeaker>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.meeting)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.speaker)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.meeting.ClosingHymn)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.speaker.SpeakerId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MeetingSpeakerId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MeetingSpeakerId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MeetingSpeakerId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
